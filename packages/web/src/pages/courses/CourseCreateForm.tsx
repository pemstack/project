import React, { FunctionComponent } from 'react'

interface CourseCreateFormProps { }

export const CourseCreateForm: FunctionComponent<CourseCreateFormProps> = ({ }) => {
  return (
    <div className='CourseCreateForm'>
      CourseCreateForm component
    </div>
  )
}
